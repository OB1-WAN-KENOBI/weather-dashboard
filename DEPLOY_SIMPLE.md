# 🚀 Простой деплой через gh-pages

## Локальный деплой (вручную)

```bash
# Собрать проект
npm run build

# Задеплоить на GitHub Pages
npm run deploy
```

Это автоматически:

1. Соберет проект (`predeploy` запустит `build`)
2. Задеплоит в ветку `gh-pages` через пакет `gh-pages`

## Автоматический деплой через GitHub Actions

После пуша в `main` автоматически запустится workflow, который:

1. Соберет проект
2. Задеплоит через `gh-pages`

## Настройка GitHub Pages

1. Settings → Pages
2. Source: выберите ветку `gh-pages` и папку `/ (root)`
3. Save

Готово! Сайт будет на: `https://ob1-wan-kenobi.github.io/weather-dashboard/`
